# VECTOR-QUERY-PROFILER
**Vector Query Profiler** is a performance analysis tool that executes SQL and vector search queries, converts text into embeddings for semantic search, measures execution time, monitors CPU/RAM/GPU usage, supports file uploads, and stores query history through an interactive dashboard. 
